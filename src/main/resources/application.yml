server:
  port: 8761
eureka:
  instance:
    hostname: 172.16.18.148
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
  server:
    enable-self-preservation: false
    renewalPercentThreshold: 0.12

